# Core
typer>=0.12
rich>=13.7
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0
httpx>=0.27
pillow>=10.3

# Provider SDKs (install the ones you use; AuraGen degrades gracefully without them)
fal-client>=0.5
replicate>=0.32
huggingface-hub>=0.24
